import webapp2from models.user_models import MyUserfrom google.appengine.ext import ndbimport loggingclass CreateUserHandler(webapp2.RequestHandler):	def post(self):		username=self.request.get('username')		regId = self.request.get('reg_id')		try:			k=MyUser.get_or_insert(username,regId=regId)			self.response.out.write(k)		except Exception,e: 			self.response.out.write(e)